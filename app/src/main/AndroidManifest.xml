<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/dado5"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dado5"
        android:supportsRtl="true"
        android:theme="@style/Theme.Juego_de_dados"
        tools:targetApi="31">

        <!-- Configura SplashActivity como la actividad principal -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de MenuActivity -->
        <activity
            android:name=".MenuActivity"
            android:exported="true" />

        <!-- Declaración de InicioActivity -->
        <activity
            android:name=".InicioActivity"
            android:exported="true" />

        <!-- Declaración de MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Juego_de_dados" />

        <!-- Declaración de RankingActivity -->
        <activity android:name=".RankingActivity" android:exported="true" />

    </application>
</manifest>
